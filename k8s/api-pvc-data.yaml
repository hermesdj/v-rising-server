apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: v-rising-api-data-pvc
  namespace: v-rising
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: glusterfs
  resources:
    requests:
      storage: 500Mi
